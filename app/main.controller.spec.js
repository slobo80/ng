// describe('MainController', function () {
//     beforeEach(module('app'));

//     it('should create a `news list` model with 3 news items', inject(function ($controller) {
//         var scope = {};
//         var ctrl = new $controller('MainController', { $scope: scope });

//         expect(scope.newsList.length).toBe(3);
//         expect(scope.newsCount).toBe(3);
//     }));

// });